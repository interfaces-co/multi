/** Dispatched when workspace / shell cwd changes (legacy string preserved for listeners). */
export const GLASS_SHELL_CHANGED_EVENT = "pi-glass-shell-changed";
/** Seeds editor text from the Glass composer (legacy string preserved for listeners). */
export const GLASS_EDITOR_SET_EVENT = "pi-glass-editor-set";
